<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mingchu.positiondetaidemo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".polymerization.PolymerizationActivity">

        </activity>
        <!--<activity android:name=".MainActivity">-->
        <!--</activity>-->
        <activity android:name=".AttentionActivity">
        </activity>
        <activity android:name=".FoodImageActivity"/>
        <activity android:name=".LaunchActivity"/>
        <activity android:name=".LiveDetailActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>